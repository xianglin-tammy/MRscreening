% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{绘制分组森林图}
\usage{
plot_forest(
  results,
  title = title,
  tsize = 8,
  asize = 3,
  zero = 1,
  cex = 2,
  boxsize = 0.2,
  output_file = NULL,
  output_format = "png",
  width = 40,
  height = 20,
  dpi = 300,
  zero_color = "red",
  zero_linetype = "dashed",
  point_shape = 15,
  point_color = "black",
  errorbar_height = 0.2,
  errorbar_color = "black",
  background_colors = c("white", "lightgrey"),
  or_column = "or",
  or_loci_column = "or_loci",
  or_upci_column = "or_upci",
  p2_column = "exposure.trait",
  p3_column = "OR_CI",
  p4_column = "mr.pval",
  breaks = c(seq(min(df$mean), max(df$mean), by = (max(df$mean) - min(df$mean))/3)),
  labels = sprintf("\%.2f", seq(min(df$mean), max(df$mean), by = (max(df$mean) -
    min(df$mean))/3)),
  x_title = "OR (95\%CI)",
  line_spacing = 1.2,
  widths = c(0.25, 0.1, 0.08, 0.05)
)
}
\arguments{
\item{results}{数据框或列表，包含OR、置信区间和分组等信息。}

\item{title}{图表标题。}

\item{tsize}{标题字体大小。}

\item{asize}{注释文本字体大小。}

\item{zero}{零线位置，通常为1。}

\item{cex}{字体缩放因子（暂未使用）。}

\item{boxsize}{点大小缩放因子。}

\item{output_file}{输出文件路径（如需保存图像）。}

\item{output_format}{输出格式，可选 "png", "jpeg", "pdf"。}

\item{width}{图像宽度（单位：厘米）。}

\item{height}{图像高度（单位：厘米）。}

\item{dpi}{分辨率，默认300。}

\item{zero_color}{零线颜色。}

\item{zero_linetype}{零线线型，默认 "dashed"。}

\item{point_shape}{点形状（如 15 为实心正方形）。}

\item{point_color}{点颜色。}

\item{errorbar_height}{误差条高度。}

\item{errorbar_color}{误差条颜色。}

\item{background_colors}{背景交替颜色，默认白色与灰色。}

\item{or_column}{OR值列名。}

\item{or_loci_column}{OR下限列名。}

\item{or_upci_column}{OR上限列名。}

\item{p2_column}{暴露信息列名。}

\item{p3_column}{OR与CI组合列名。}

\item{p4_column}{P值列名。}

\item{breaks}{X轴刻度线位置（数值向量）。}

\item{labels}{X轴刻度标签（与 breaks 对应）。}

\item{x_title}{X轴标题。}

\item{line_spacing}{行间距设置（暂未使用）。}

\item{widths}{各列宽度占比。}
}
\value{
无返回值，绘图或保存图像。
}
\description{
该函数用于绘制带有分组信息和多个变量的森林图，支持误差条、点估计、暴露信息、OR区间与p值列的展示，并可导出为图片。
}
